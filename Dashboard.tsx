import React from 'react';

// This component is not currently used in the application and was causing build errors.
// It has been stubbed out to resolve these issues. The functionality it provided
// has been superseded by the LiveOpportunities component.
export const Dashboard: React.FC = () => {
  return (
    <div className="p-4 md:p-8">
      <h2 className="text-2xl font-bold">Dashboard (Deprecated)</h2>
      <p className="text-gray-400 mt-2">This component is no longer in use.</p>
    </div>
  );
};
